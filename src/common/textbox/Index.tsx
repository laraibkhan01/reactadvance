import React from "react";

interface TextBoxProps {
  label: string;
  palceholder: string;
  value: string;
  onChange: () => void;
}
const TextBox = (props: TextBoxProps) => {
  return <div></div>;
};

export default TextBox;
